#include <bits/stdc++.h>
using namespace std;

int main()
{
    vector<int> arr;
    int a;
    do
    {
        cin >> a;
        if (a != -1)
            cout << a << " ";
    } while (a != -1);

    return 0;
}